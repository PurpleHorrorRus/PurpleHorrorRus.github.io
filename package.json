{
    "private": true,
    "scripts": {
        "dev": "nuxi dev",
        "build": "nuxi build",
        "start": "node .output/server/index.mjs",
        "deploy": "cross-env NODE_ENV=production nuxi build && push-dir --dir=.output --branch=gh-pages --cleanup"
    },
    "devDependencies": {
        "cross-env": "^7.0.3",
        "nuxt3": "latest",
        "push-dir": "^0.4.1",
        "sass": "^1.49.0",
        "vite-svg-loader": "^3.1.2"
    },
    "dependencies": {}
}
