{
    "private": true,
    "scripts": {
        "dev": "nuxi dev",
        "build": "nuxi build",
        "start": "node .output/server/index.mjs",
        "deploy": "cross-env NODE_ENV=production nuxt generate && push-dir --dir=dist --branch=gh-pages --cleanup"
    },
    "devDependencies": {
        "cross-env": "^7.0.3",
        "nuxt3": "latest",
        "sass": "^1.49.0",
        "vite-svg-loader": "^3.1.2"
    },
    "dependencies": {}
}
